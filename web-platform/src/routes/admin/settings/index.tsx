import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminSettingsPage() {
  return (
    <PlaceholderPage
      title="Account 设置"
      plannedIn="P4-E1"
      description="v0.1 仅展示本 Account 基本信息占位，不承载正式功能（06 §13.2）。"
    />
  );
}
