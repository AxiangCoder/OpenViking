import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminRecycleBinPage() {
  return (
    <PlaceholderPage
      title="回收站（Account）"
      plannedIn="P4-E3"
      description="当前 Account 可恢复对象，按类型恢复（与 /app/recycle-bin 共用后端与组件）。"
    />
  );
}
