import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminUserApiKeysPage() {
  return (
    <PlaceholderPage
      title="成员 API Keys（只读元数据）"
      plannedIn="P4-E2"
      description="credential.read.account：只显示元数据与掩码，绝不展示明文（06 §13.5）。"
    />
  );
}
