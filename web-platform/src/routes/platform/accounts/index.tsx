import PlaceholderPage from "@/components/PlaceholderPage";

export default function PlatformAccountsPage() {
  return (
    <PlaceholderPage
      title="Accounts"
      plannedIn="P4-E4"
      description="全部 Account 列表/创建（account.manage.platform）；选择目标 Account 是查看管理对象，不改变 Actor 身份（06 §13.2）。"
    />
  );
}
