import PlaceholderPage from "@/components/PlaceholderPage";

export default function PlatformAccountUsersPage() {
  return (
    <PlaceholderPage
      title="Account 用户"
      plannedIn="P4-E4"
      description="目标 Account 用户列表与角色提升（user.read.platform / role.assign.platform）。"
    />
  );
}
