import PlaceholderPage from "@/components/PlaceholderPage";

export default function PlatformAccountSkillsPage() {
  return (
    <PlaceholderPage
      title="Account Skill"
      plannedIn="P4-E4"
      description="目标 Account Skill 只读视图（skill.*.read.platform，PSA 对 Skill 只读）。"
    />
  );
}
