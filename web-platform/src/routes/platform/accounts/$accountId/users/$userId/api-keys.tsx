import PlaceholderPage from "@/components/PlaceholderPage";

export default function PlatformAccountUserApiKeysPage() {
  return (
    <PlaceholderPage
      title="成员 API Keys（只读元数据）"
      plannedIn="P4-E4"
      description="credential.read.platform：只显示元数据与掩码，绝不展示明文。"
    />
  );
}
