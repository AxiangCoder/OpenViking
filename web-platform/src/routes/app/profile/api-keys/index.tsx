import PlaceholderPage from "@/components/PlaceholderPage";

export default function ProfileApiKeysPage() {
  return (
    <PlaceholderPage
      title="API Keys"
      plannedIn="P3-E2"
      description="列表掩码、创建、撤销、一次性明文展示页；Key 明文仅内存态（06 §13.5）。"
    />
  );
}
